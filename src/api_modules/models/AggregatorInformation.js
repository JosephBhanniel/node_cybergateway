class AggregatorInformation {
    constructor(subMerchant, name, aggregatorID) {
        this.subMerchant = subMerchant;
        this.name = name;
        this.aggregatorID = aggregatorID;
    }
}

module.exports = AggregatorInformation;
